"""
This package contains the use case layer of the application.

The use case layer is responsible for implementing the application-specific
business rules. It orchestrates the interaction between the domain layer and
external systems, ensuring that the application's behavior aligns with its
business requirements.

Components in this package include use case implementations and application services.
"""
